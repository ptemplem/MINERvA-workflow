stages:
- name: test
  dependencies: [init]
  scheduler:
    scheduler_type: multistep-stage
    parameters:
      mc_runs: {step: init, output: mc_runs}
      outdir: '{workdir}'
    scatter:
      method: zip
      parameters: [mc_runs]
    step: {$ref: 'steps.yml#/make_histograms'}